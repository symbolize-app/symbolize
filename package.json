{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "fmt": "prettier --ignore-path .gitignore --write **/*.js **/*.json **/*.ts **/*.html"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "snowpack": "^3.0.11"
  },
  "prettier": {
    "printWidth": 60,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  }
}
