version: '3'
run: when_changed
tasks:
  check:
    cmd: buck2 build //lib-markup:check
  format:check:
    aliases:
    - fc
    cmd: buck2 build //lib-markup:format-check
  test:browser:
    deps:
    - build
    dir: '{{.TASKFILE_DIR}}'
    cmd: gleam run --module lib_markup_browser_driver_main
  build:
    dir: '{{.TASKFILE_DIR}}'
    cmd: gleam build
  link-build-dir:
    cmd:
      task: :build-dir:link-package-build-dir
      vars:
        NAME: '{{.NAME}}'
  lint:
    aliases:
    - l
    cmd: buck2 build //lib-markup:lint
  test:debug:
    aliases:
    - test
    - t
    cmd: buck2 test -m debug //lib-markup:test
  test:release:
    aliases:
    - tr
    cmd: buck2 test -m release //lib-markup:test
vars:
  NAME: lib-markup
