# Duplicate dependency ledger for the direct-path vendor closure.
# Unresolved duplicates belong in [[duplicate]] entries. The absence of one
# means that no duplicate snapshot identities are currently retained.
version = 1
