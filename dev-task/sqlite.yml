version: '3'

run: when_changed

tasks:
  lint:check:
    aliases:
      - lint
      - l
    cmd: sqlfluff lint --nocolor .

  lint:fix:
    aliases:
      - lf
    cmd: sqlfluff fix --nocolor --force .
